package logic.input;

/**
 * Enum representing possible input commands for the game.
 */
public enum InputCommand {
    MOVE_UP,
    MOVE_DOWN,
    MOVE_LEFT,
    MOVE_RIGHT,
    UNDO,
    REDO,
    RESET,
    MENU,
    WAIT,
    NONE
}
